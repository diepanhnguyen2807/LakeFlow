# ======================
# EDUAI DATA LAKE CONFIG
# ======================
EDUAI_DATA_PATH=/data
QDRANT_PORT=6333

# Dùng docker
#EDUAI_DATA_BASE_PATH=/data
#QDRANT_HOST=qdrant
#API_BASE_URL=http://lakeflow-backend:8011


# Không dùng docker (backend đọc LAKEFLOW_DATA_BASE_PATH, không phải EDUAI_*)
LAKEFLOW_DATA_BASE_PATH=/data
EDUAI_DATA_BASE_PATH=/data
HOST_SYNOLOGY_PATH=/Users/mac/Library/CloudStorage/SynologyDrive-education/
QDRANT_HOST=localhost
API_BASE_URL=http://localhost:8011

# Q&A / Admission agent: URL Ollama (hoặc OpenAI-compatible). Server chạy LakeFlow PHẢI kết nối được tới URL này.
# Nếu deploy trên máy nội bộ không ra được research.neu.edu.vn → set URL Ollama nội bộ (vd. http://ollama-host:11434 hoặc http://10.2.13.xx:11434).
# Lỗi "No route to host" khi gọi /ask = LLM_BASE_URL không reachable từ server LakeFlow.
LLM_BASE_URL=https://research.neu.edu.vn/ollama
LLM_MODEL=qwen3:8b
# Dùng v1/chat/completions (OpenAI-compatible). Để dùng /api/chat thì set=true
# USE_OLLAMA_NATIVE_CHAT=true

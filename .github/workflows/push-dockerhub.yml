# Build và đẩy LakeFlow (backend + frontend) lên Docker Hub.
# Kích hoạt: push lên main hoặc chạy thủ công (Actions → Push LakeFlow to Docker Hub → Run workflow).
#
# Secrets: DOCKERHUB_USER, DOCKERHUB_TOKEN

name: Push LakeFlow to Docker Hub

on:
  push:
    branches: ['main']
    paths:
      - 'lake-flow/**'
      - 'lake-flow-ui/**'
      - '.github/workflows/push-dockerhub.yml'
  workflow_dispatch:

jobs:
  backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USER }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - uses: docker/build-push-action@v6
        with:
          context: ./lake-flow
          push: true
          tags: ${{ secrets.DOCKERHUB_USER }}/lakeflow-backend:latest
          platforms: linux/amd64
          cache-from: type=gha
          cache-to: type=gha,mode=max

  frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USER }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - uses: docker/build-push-action@v6
        with:
          context: ./lake-flow-ui
          push: true
          tags: ${{ secrets.DOCKERHUB_USER }}/lakeflow-frontend:latest
          platforms: linux/amd64
          cache-from: type=gha
          cache-to: type=gha,mode=max
